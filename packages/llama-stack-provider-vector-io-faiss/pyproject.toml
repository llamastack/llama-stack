[project]
name = "llama-stack-provider-vector-io-faiss"
dynamic = ["version"]
requires-python = ">=3.12"
dependencies = [
    "llama-stack",
    "llama-stack-api",
    "llama-stack-utils-vector-io",
]
readme = "README.md"
license = { file = "LICENSE" }
description = "Llama Stack vector I/O provider: faiss"

[project.entry-points."llama_stack.providers"]
    "vector_io.inline.faiss" = "llama_stack_provider_vector_io_faiss.provider:get_provider_spec"
    "vector_io.inline.builtin" = "llama_stack_provider_vector_io_faiss.provider:get_deprecated_builtin_spec"

[build-system]
requires = ["setuptools>=74.0", "setuptools-scm>=8.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
root = "../.."
fallback_version = "0.7.2.dev0"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
