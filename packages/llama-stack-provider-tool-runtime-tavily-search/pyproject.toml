[build-system]
requires = ["setuptools>=61.0", "setuptools-scm>=8.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
root = "../.."
fallback_version = "0.7.1.dev0"

[tool.uv.sources]
llama-stack = { workspace = true }
llama-stack-api = { workspace = true }

[project]
name = "llama-stack-provider-tool-runtime-tavily-search"
dynamic = ["version"]
description = "Tavily Search tool for AI-optimized web search with structured results."
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "llama-stack",
    "llama-stack-api",
    "requests",
]

[project.entry-points."llama_stack.providers"]
"tool_runtime.remote.tavily-search" = "llama_stack_provider_tool_runtime_tavily_search.provider:get_provider_spec"

[tool.setuptools.packages.find]
where = ["src"]
include = ["llama_stack_provider_tool_runtime_tavily_search"]
