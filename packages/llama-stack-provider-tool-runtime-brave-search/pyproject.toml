[build-system]
requires = ["setuptools>=61.0", "setuptools-scm>=8.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
root = "../.."
fallback_version = "0.7.1.dev0"

[tool.uv.sources]
llama-stack = { workspace = true }
llama-stack-api = { workspace = true }

[project]
name = "llama-stack-provider-tool-runtime-brave-search"
dynamic = ["version"]
description = "Brave Search tool for web search capabilities with privacy-focused results."
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "llama-stack",
    "llama-stack-api",
    "requests",
]

[project.entry-points."llama_stack.providers"]
"tool_runtime.remote.brave-search" = "llama_stack_provider_tool_runtime_brave_search.provider:get_provider_spec"

[tool.setuptools.packages.find]
where = ["src"]
include = ["llama_stack_provider_tool_runtime_brave_search"]
